@{
    ViewData["Title"] = "Phân Công Nhiệm Vụ";
    var users = ViewBag.Users as List<SelectListItem>;
    var subjects = ViewBag.Subjects as List<SelectListItem>;
    Layout = "~/Views/Shared/_TruongBoMon.cshtml"; // hoặc _GiangVien.cshtml nếu bạn có
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-7">
            <div class="card shadow border-0">
                <div class="card-header bg-gradient bg-primary text-white text-center">
                    <h4 class="mb-0">
                        <i class="fas fa-tasks me-2"></i>Phân Công Nhiệm Vụ Cho Giảng Viên
                    </h4>
                </div>
                <div class="card-body">
                    <form asp-action="PhanCongNhiemVu" method="post">
                        <div class="mb-3">
                            <label class="form-label fw-semibold">👨‍🏫 Giảng viên</label>
                            <select name="AssignedTo" class="form-select" required>
                                <option value="">-- Chọn giảng viên --</option>
                                @foreach (var user in users)
                                {
                                    <option value="@user.Value">@user.Text</option>
                                }
                            </select>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold">📘 Môn học</label>
                            <select name="SubjectId" class="form-select" required>
                                <option value="">-- Chọn môn học --</option>
                                @foreach (var subject in subjects)
                                {
                                    <option value="@subject.Value">@subject.Text</option>
                                }
                            </select>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold">📝 Mô tả nhiệm vụ</label>
                            <textarea name="TaskDescription" class="form-control" rows="4" placeholder="Nhập nội dung nhiệm vụ..." required></textarea>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold">📅 Hạn nộp</label>
                            <input name="DueDate" type="date" class="form-control" required />
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-success btn-lg">
                                <i class="fas fa-paper-plane me-1"></i> Phân công
                            </button>
                        </div>
                    </form>

                    @if (TempData["Success"] != null)
                    {
                        <div class="alert alert-success alert-dismissible fade show mt-4" role="alert">
                            <i class="fas fa-check-circle me-2"></i> @TempData["Success"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
