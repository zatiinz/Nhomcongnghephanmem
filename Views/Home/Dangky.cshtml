@model WeBQBCA.Data.User

@{
    ViewData["Title"] = "Đăng ký";
    Layout = "~/Views/Shared/_LgingLgout.cshtml";
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QBCA - Đăng ký tài khoản</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/style.css" />
</head>
<body class="login-page">
    <main class="login-container">
        <header class="login-header">
            <div class="logo-circle">
                <i class="fas fa-user-plus"></i>
            </div>
            <h1>QBCA</h1>
            <p>Đăng ký tài khoản giảng viên hoặc admin</p>
        </header>

        <section class="login-content">
            <form asp-action="Dangky" method="post" class="login-form">
                <div asp-validation-summary="ModelOnly" class="text-danger text-center mb-2"></div>

                <div class="form-group">
                    <label class="form-label">Tên đăng nhập</label>
                    <div class="input-with-icon">
                        <i class="fas fa-user"></i>
                        <input asp-for="Username" class="form-control" placeholder="Nhập tên đăng nhập" />
                    </div>
                    <span asp-validation-for="Username" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label class="form-label">Mật khẩu</label>
                    <div class="input-with-icon">
                        <i class="fas fa-lock"></i>
                        <input asp-for="Password" type="password" class="form-control" placeholder="Nhập mật khẩu" />
                    </div>
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label class="form-label">Vai trò</label>
                    <div class="input-with-icon">
                        <i class="fas fa-user-tag"></i>
                        <input asp-for="Role" class="form-control" placeholder="Nhập vai trò: Admin / GiangVien" />
                    </div>
                    <span asp-validation-for="Role" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label class="form-label">Bộ môn</label>
                    <div class="input-with-icon">
                        <i class="fas fa-book"></i>
                        <input asp-for="Subject" class="form-control" placeholder="Nhập tên bộ môn (nếu có)" />
                    </div>
                    <span asp-validation-for="Subject" class="text-danger"></span>
                </div>

                <button type="submit" class="btn btn-primary btn-block">
                    <span class="btn-text">Đăng ký</span>
                    <i class="fas fa-user-plus btn-icon"></i>
                </button>
            </form>
        </section>

        <footer class="login-footer">
            <p>© 2023 QBCA - FPT University. Phiên bản 1.0.0</p>
            <div class="language-selector">
                <i class="fas fa-globe"></i>
                <select aria-label="Chọn ngôn ngữ">
                    <option value="vi" selected>Tiếng Việt</option>
                    <option value="en">English</option>
                </select>
            </div>
        </footer>
    </main>

    <div class="login-decoration">
        <div class="circle circle-1"></div>
        <div class="circle circle-2"></div>
    </div>

    <script src="~/js/login.js"></script>
</body>
</html>

@section Scripts {
    @{
    }
}

